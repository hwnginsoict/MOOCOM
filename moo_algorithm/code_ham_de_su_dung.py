'''
Một số hàm em cần code:
problem là do em tự định nghĩa, còn a cần các hàm này để truyền vào thuật toán khi chạy
1. create_solution(problem) hàm này trả về 1 chromosome (solution được mã hóa)
2. crossover_operator(problem, parent1, parent2) hàm này trả về 2 Individual con không cần tính fitness
trong hàm làm gì tùy em, crossover + repair, ...
3. mutation_operator(problem, individual) hàm này trả về 1 Individual con không cần tính fitness
4. cal_fitness(problem, individual) hàm này tính fitness cho individual trả về objectives (đa mục tiêu)
'''